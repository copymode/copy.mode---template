
// This is a small component to export the Upload icon from lucide-react
// since it was used in the ProfileCard but wasn't imported directly
import { Upload as UploadIcon } from "lucide-react";

export const Upload = UploadIcon;
